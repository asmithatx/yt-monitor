# ============================================================
#  yt-monitor — Environment Configuration
#  Copy this file to .env and fill in your values.
#  Never commit .env to version control.
# ============================================================

# ── Anthropic ──────────────────────────────────────────────
ANTHROPIC_API_KEY=your_anthropic_api_key_here
# Model to use for summarization. Verify exact string at:
# https://docs.anthropic.com/en/docs/about-claude/models
CLAUDE_MODEL=claude-sonnet-4-5-20250929

# ── Trello ─────────────────────────────────────────────────
# Get your key at: https://trello.com/power-ups/admin
# Then visit: https://trello.com/1/authorize?expiration=never&scope=read,write&response_type=token&key=YOUR_KEY
TRELLO_API_KEY=your_trello_api_key_here
TRELLO_TOKEN=your_trello_token_here
# The ID of the Trello list where cards will be created
# (right-click a list in Trello browser → inspect → find idList, or use the API explorer)
TRELLO_LIST_ID=your_trello_list_id_here
# Optional: label IDs to apply to new cards (comma-separated)
TRELLO_LABEL_IDS=

# ── YouTube Data API (DISABLED by default) ─────────────────
# Only needed if YOUTUBE_API_ENABLED=true in config.py
# Get a key at: https://console.cloud.google.com/
YOUTUBE_API_KEY=your_youtube_api_key_here

# ── Proxy Settings (DISABLED by default) ───────────────────
# Only used if PROXY_ENABLED=true in config.py
# Webshare-compatible proxy credentials (https://proxy.webshare.io)
PROXY_USERNAME=
PROXY_PASSWORD=

# ── Application ────────────────────────────────────────────
# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
# Path where SQLite database file is stored (inside container: /app/data/monitor.db)
DATABASE_PATH=/app/data/monitor.db
